{
  "name": "job-hunt",
  "private": true,
  "version": "0.1.0",
  "description": "Personal job-search backend: scrape, tailor, human-review, publish (prepare-only)",
  "type": "module",
  "scripts": {
    "job-hunt": "bun run src/cli.ts",
    "sync": "bun run scripts/sync-framework.ts",
    "test": "bun test"
  },
  "dependencies": {
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0"
  }
}
